{
  "version": 2,
  "builds": [
    {
      "src": "**",
      "use": "@vercel/python",
      "config": {
        "runtime": "python3.9",
        "installCommand": "pip install -r requirements.txt",
        "buildCommand": "flask run",
        "outputDirectory": "dist"
      }
    }
  ]
}
